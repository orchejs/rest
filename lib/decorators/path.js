// @flow
// import { RouterLoader } from '../loaders/routerloader';

export default function Path() {
  return () => {
    // const className = target.toString().match(/function ([^(]*)/)[1];
    // RouterLoader.addRouterConfig(className, path);
  };
}
