import { expect } from 'chai';
import { PathUtils } from '../../lib/utils/path.utils';

describe('PathUtils', () => {
  
  describe('#urlSanitation', () => {
    it('', () => {
      PathUtils.urlSanitation();

    });
  });
});
