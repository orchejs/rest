import { ExpressEngine } from '../../lib/engines/express.engine';

/*
describe('PackageUtils', () => {
  let exEngine: ExpressEngine;

  before(() => {
    exEngine = new ExpressEngine();
  });

  describe('#loadOrcheConfig', () => {
    it('Should return false if version is not compatible', () => {

    });
  });

  describe('#loadServer', () => {
    it('should return the app description from package.json', () => {
      const description = exEngine.getDescrition();

      if (!description) {
        throw new Error(`Expected a description for the app, but was ${description}`);
      }
    });
  });
});
*/
